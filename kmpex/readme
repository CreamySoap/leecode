kmp算法的优化版本
把理解就写在这里面了 流程如下：
1 获取模式串的next数组:
1)其中next[j]有几个意义:
表示除去当前字符之前的模式串的最大前后缀长度 开始时 next【0】=-1；//例如假设next[5]=2; 表示除去模式串中下标为5的字符串后前面0-4的
5个字符中有最大前后缀长度为2 所以下标为5的字符失配时应该用模式串中第3个字符 也就是下标为2字符去进行匹配；
表示当前字符失配时 下一次应该用模式串中下标为next[j]的字符来进行匹配；
2）第一步 当k==-1 表示当前字符串中除去当前失配字符后 没有前后缀了  简单的i++ J++ 用模式串第一个字符和下一个i匹配就行；
此时 next[j]=0; 当
